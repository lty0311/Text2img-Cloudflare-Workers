name = "text2img"
main = "worker.js"
compatibility_date = "2026-05-13"

# AI 绑定（官方要求必须写 [ai] + remote = true）
[ai]
binding = "AI"
remote = true

# 静态文本规则（正确导入 index.html）
[[rules]]
type = "Text"
globs = ["**/*.html"]
